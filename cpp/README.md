## Install C++ build tools

### Install c++ build tools in linux

```bash
sudo apt update && sudo apt install build-essential
```

## Compile the Code in executable

```bash
g++ main.cpp -o main
```

## Run the executable

```bash
./main

```
